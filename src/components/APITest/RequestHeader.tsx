import * as React from 'react';
import EditableTable from './Editable/Editable';

const RequestHeader: React.FC = () => {
    return (
        <div>
            <EditableTable />
        </div>
    );
}

export default RequestHeader;